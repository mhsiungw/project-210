appId: com.example.electronapp
productName: Proejct-210
directories:
  buildResources: build
  output: dist
files:
  - out/**
  - package.json
  - '!src/**'
  - '!*.config.*'
  - '!tsconfig*'
  - '!.eslint*'
  - '!.prettier*'
mac:
  target:
    - target: dmg
      arch: [x64, arm64]
  category: public.app-category.developer-tools
  icon: ../shared/src/assets/logo.png
win:
  target:
    - target: nsis
      arch: [x64]
  icon: ../shared/src/assets/logo.png
linux:
  target:
    - target: AppImage
      arch: [x64]
  icon: ../shared/src/assets/logo.png
nsis:
  artifactName: ${productName}-${version}-setup.${ext}
  oneClick: false
  allowToChangeInstallationDirectory: true
npmRebuild: false
