name: Deploy Web

on:
  workflow_dispatch:
    inputs:
      environment:
        description: 'Target environment'
        required: true
        default: staging
        type: choice
        options: [staging, production]
  workflow_call:
    inputs:
      environment:
        required: true
        type: string

concurrency:
  group: deploy-web-${{ github.ref }}-${{ inputs.environment }}
  cancel-in-progress: false

jobs:
  deploy-staging:
    name: Deploy → staging
    if: inputs.environment == 'staging'
    runs-on: ubuntu-latest
    timeout-minutes: 15
    environment: staging
    steps:
      - uses: actions/checkout@v6

      - uses: pnpm/action-setup@v4
        with:
          version: 9.15.0

      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: pnpm

      - run: pnpm install --frozen-lockfile

      - name: Materialise env/.env.staging and export Vercel credentials
        env:
          WEB_ENV_STAGING: ${{ secrets.WEB_ENV_STAGING }}
        run: |
          if [ -z "$WEB_ENV_STAGING" ]; then
            echo "::error::WEB_ENV_STAGING secret is empty or unset"
            exit 1
          fi
          printf '%s\n' "$WEB_ENV_STAGING" > env/.env.staging

          set -a
          # shellcheck disable=SC1091
          . env/.env.staging
          set +a
          for var in VERCEL_ORG_ID VERCEL_PROJECT_ID VERCEL_TOKEN \
                     VITE_API_URL VITE_SUPABASE_URL VITE_SUPABASE_ANON_KEY; do
            if [ -z "${!var:-}" ]; then
              echo "::error::$var missing from WEB_ENV_STAGING"
              exit 1
            fi
            case $var in
              VERCEL_*)
                echo "::add-mask::${!var}"
                echo "$var=${!var}" >> "$GITHUB_ENV"
                ;;
            esac
          done

      - name: Install Vercel CLI
        run: pnpm add -g vercel@latest

      - name: Build staging bundle
        run: pnpm -F @app/web build:staging

      - name: Deploy to staging
        id: deploy
        working-directory: packages/web
        run: |
          url=$(vercel deploy --token="$VERCEL_TOKEN")
          echo "url=$url" >> "$GITHUB_OUTPUT"

      - name: Alias deployment to staging-web-1ace3.vercel.app
        working-directory: packages/web
        run: vercel alias --token="$VERCEL_TOKEN" "${{ steps.deploy.outputs.url }}" staging-web-1ace3.vercel.app

  deploy-production:
    name: Deploy → production
    if: inputs.environment == 'production'
    runs-on: ubuntu-latest
    timeout-minutes: 15
    environment: production
    steps:
      - uses: actions/checkout@v6

      - uses: pnpm/action-setup@v4
        with:
          version: 9.15.0

      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: pnpm

      - run: pnpm install --frozen-lockfile

      - name: Materialise env/.env.production and export Vercel credentials
        env:
          WEB_ENV_PRODUCTION: ${{ secrets.WEB_ENV_PRODUCTION }}
        run: |
          if [ -z "$WEB_ENV_PRODUCTION" ]; then
            echo "::error::WEB_ENV_PRODUCTION secret is empty or unset"
            exit 1
          fi
          printf '%s\n' "$WEB_ENV_PRODUCTION" > env/.env.production

          set -a
          # shellcheck disable=SC1091
          . env/.env.production
          set +a
          for var in VERCEL_ORG_ID VERCEL_PROJECT_ID VERCEL_TOKEN \
                     VITE_API_URL VITE_SUPABASE_URL VITE_SUPABASE_ANON_KEY; do
            if [ -z "${!var:-}" ]; then
              echo "::error::$var missing from WEB_ENV_PRODUCTION"
              exit 1
            fi
            case $var in
              VERCEL_*)
                echo "::add-mask::${!var}"
                echo "$var=${!var}" >> "$GITHUB_ENV"
                ;;
            esac
          done

      - name: Install Vercel CLI
        run: pnpm add -g vercel@latest

      - name: Build and deploy to production
        run: pnpm deploy:web:prod
