# Template for `env/.env.<environment>.local` files (secrets). COMMITTED as a template.
# The real .env.<env>.local files are GITIGNORED and also stored in CI secrets.
# Copy to env/.env.development.local / .env.staging.local / .env.production.local.

# --- Database ---
DATABASE_URL=
DIRECT_URL=

# --- Storage ---
# PEM private key; keep newlines, wrap the whole value in double quotes.
CLOUDFRONT_PRIVATE_KEY=

# --- Supabase anon key (treated as secret in this project, mirrored to VITE_) ---
SUPABASE_ANON_KEY=
VITE_SUPABASE_ANON_KEY=

# --- AWS access keys ---
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=

# --- Sentry source-map upload (build-time only) ---
SENTRY_AUTH_TOKEN=
