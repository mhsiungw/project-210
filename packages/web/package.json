{
  "name": "@app/web",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "typecheck": "tsc --build",
    "deploy": "vite build --mode staging && vercel deploy",
    "deploy:prod": "vite build --mode production && vercel deploy --prod"
  },
  "dependencies": {
    "@app/shared": "workspace:*"
  }
}
